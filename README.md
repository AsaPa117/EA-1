# EA-1
Extracurricular Assignment 01

Deliverable-Specific Content:
The link below is a repository for a project that I've done at Hackathon. Its a fitness application that determines user best type of workout.
link: https://github.com/AsaPa117/Hackathon.git

File Description: The fitness application is written in Python and uses a machine learning algorithm, k-means clustering, as its core. In the project, we have only two main files: FitnessPlanUI and FitnessPlan. In the FitnessPlanUI, it creates a window where the user can interact with the program. The window displays questions the user must answer and, using the given data, prints a result. However, before the program returns a result, it must process the user's data in another file, fitnessPlan. The fitnessPlan file uses an imported function to create a machine learning algorithm. It takes in generic samples of categorial data and plots them into a clustered graph. When the ML begins to show visible clusters, it can produce accurate results and is able to process user data. Once done, it sends the result to the UI file and, depending on the result, displays the user's fitness goal. 
Running the Program: If you want to test the application, you can clone the link on a local computer. However, before you can run the program, you might need to download and import libraries for KModes ML, PySide, and Python from the terminal. Once downloaded, you can use Visual Studio Code to start it off. 

Proof of Participation:


Reflection
Activity Description: In a hackathon, the topic of our project solely focused on "Reinventing the Wheel", meaning we had to recreate an already existing product or concept that we could alter. Our team and I decided to recreate a fitness plan app, but to implement machine learning and challenge ourselves to write it in Python instead of C/C++. 

Contribution: The project was done in a team. My role was to establish the overall structure of our project. I set up the application workflow, created questionnaires, researched machine learning, supported teammates with their tasks, connected all teammates' work (libraries, files, packages) into a single repository, and debugged/tested the project.  

Technical Decisions: There were major issues with downloading packages and linking files. There was an issue with our teammate's computer: they couldn't locate or even download the necessary components in their terminal. Also, each teammate was assigned a different task and imported files that they needed, whether they were attachments from Visual Studio or external downloads. So we couldn’t test our program together. I told them to continue working on their own tasks separately and test them on their local computers. When they're done, I could clone our work together and download the packages in a virtual environment so the code can access everyone's libraries and debug everyone's work.  

Quality Assessment: Joining a hackathon was a great experience. It allowed me to engage with people with diverse skill sets and gain perspective on different viewpoints. If I were to redo the event, I would re-establish our goal, set up a guarantee plan before coding, address the issues with the approaches we had already worked on, and then replace it. 

